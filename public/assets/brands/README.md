# Brand Assets

Place manually collected brand logo files here.

Use stable local paths such as `/assets/brands/intel.svg`.

Do not hotlink external logos. If a logo file is missing, the UI falls back to a compact initials badge.
